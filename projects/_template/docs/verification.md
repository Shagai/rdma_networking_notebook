# Verification

Keep this file current as the implementation evolves. Verification should prove
both correctness and the intended learning outcome.

## Build

```sh
# Replace with the project build command.
make
```

Expected result:

- The build completes without warnings that hide correctness issues.

## Run

```sh
# Replace with the project run command.
./starter/bin/project
```

Expected result:

- The program starts and reports the current project state.

## Correctness Checks

- Record the command that sends or receives data.
- Record the expected payload, status, or completion output.
- Record how failures are surfaced.

## Cleanup

```sh
# Replace with the cleanup command if needed.
make clean
```

Expected result:

- Generated build artifacts are removed.
- No runtime state is left behind unexpectedly.
